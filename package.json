{
  "name": "bajaj-bfhl",
  "version": "1.0.0",
  "description": "BFHL Hierarchical Data Processor",
  "scripts": {
    "build": "cd server && npm install && cd ../client && npm install --include=dev && npm run build",
    "start": "cd server && node index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
